apiVersion: v1
kind: Service
metadata:
  name: balance-postgres
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9187"
spec:
  type: NodePort  # Для доступа снаружи
  selector:
    app: balance-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30033  # Будет доступен на localhost:30033

    - name: metrics
      port: 9187
      targetPort: 9187